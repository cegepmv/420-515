baseURL = 'https://cegepmv.github.io/420-515/'
languageCode = 'fr-ca'
title = 'Maintenance de logiciel'
theme = 'hugo-theme-relearn'
enableEmoji = true
[params]
  # Change default color scheme with a variant one. Can be empty, "red", "blue", "green".
  themeVariant = ["green", "relearn-light", "relearn-dark", "blue", "red"]
  disableLandingPageButton = true
  alwaysopen = false
  # Set this to true to disable copy-to-clipboard button for inline code.
  disableInlineCopyToClipBoard = true
  # Désactiver search bar
  # disableSearch = true
  # Hide breadcrumbs in the header and only show the current page title
  disableBreadcrumb = false
  # If set to true, hide table of contents menu in the header of all pages
  disableToc = true
  # Hide Next and Previous page buttons normally displayed full height beside content
  disableNextPrev = true
#comment

[markup]
  [markup.goldmark]
    [markup.goldmark.renderer]
      unsafe = true

